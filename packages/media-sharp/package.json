{
  "name": "@spectra/media-sharp",
  "version": "0.1.0",
  "private": true,
  "description": "Sharp-backed ImageRenderer adapter for the media-core image port. Real image processing (resize/crop/rotate/overlay/format) with tenant-rooted storage. Video/audio stay honestly unavailable until ffmpeg is wired.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run src",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@spectra/contracts": "workspace:*",
    "@spectra/media-core": "workspace:*",
    "@spectra/storage": "workspace:*",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
