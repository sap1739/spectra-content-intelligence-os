{
  "name": "@spectra/content-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-grounded content drafting. Builds prompts that separate trusted instructions from wrapped untrusted evidence, calls an ai-core TextGenerationProvider, and returns drafts carrying exactly the citations they were grounded on.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run src",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@spectra/ai-core": "workspace:*",
    "@spectra/contracts": "workspace:*",
    "@spectra/database": "workspace:*",
    "@spectra/knowledge-core": "workspace:*",
    "@spectra/logging": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
