{
  "name": "@spectra/metering",
  "version": "0.1.0",
  "private": true,
  "description": "Usage metering: an append-only ledger of real provider spend plus versioned cost ESTIMATES. Records only what providers actually report.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run src",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@spectra/contracts": "workspace:*",
    "@spectra/database": "workspace:*",
    "@spectra/logging": "workspace:*"
  },
  "devDependencies": {
    "@spectra/ai-voyage": "workspace:*",
    "@spectra/config": "workspace:*",
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
