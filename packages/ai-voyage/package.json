{
  "name": "@spectra/ai-voyage",
  "version": "0.1.0",
  "private": true,
  "description": "Voyage AI adapter for the ai-core EmbeddingProvider port — real semantic embeddings, env-gated with an honest unavailable state.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run src",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@spectra/ai-core": "workspace:*",
    "@spectra/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
