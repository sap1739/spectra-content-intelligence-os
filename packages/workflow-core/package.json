{
  "name": "@spectra/workflow-core",
  "version": "0.1.0",
  "private": true,
  "description": "Queue-neutral job orchestration ports with BullMQ and in-memory adapters.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@spectra/logging": "workspace:*",
    "@spectra/observability": "workspace:*",
    "bullmq": "^5.50.0",
    "ioredis": "5.10.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
