{
  "name": "@spectra/social-wordpress",
  "version": "0.1.0",
  "private": true,
  "description": "Real WordPress publisher: posts via the WordPress REST API with application-password Basic auth. The first live platform adapter — only publishes when a site + credential are configured.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run src",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@spectra/contracts": "workspace:*",
    "@spectra/social-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
