{
  "name": "@spectra/research-brave",
  "version": "0.1.0",
  "private": true,
  "description": "Brave Search adapters for the research-core WebSearchProvider and NewsSearchProvider ports — real web/news discovery, env-gated with an honest unavailable state.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run src",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@spectra/contracts": "workspace:*",
    "@spectra/research-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
